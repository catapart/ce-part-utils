

export default {
    'should have all panels': async () =>
    {
        const editor = document.querySelector("gameframe-editor");
        // await expect(editor).toBeDefined();
    },
    'should allow panel navigation': async () =>
    {
        // const editor = document.querySelector("gameframe-editor");
        // await expect(editor).toBeDefined();
    },
}